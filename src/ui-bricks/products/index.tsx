"use client";

import { FC, memo } from "react";

type Props = {
    products: any;
};

export const Products: FC<Props> = memo(({ products }) => {
    return <></>;
});

Products.displayName = "Products";
